export const sendEmail = async (to, subject, message) => {
    // Placeholder: integrate real email provider later (e.g., Brevo, SendGrid)
    console.log("📧 Sending email to:", to, "Subject:", subject);
    console.log("Message:", message);

    return true;
};
